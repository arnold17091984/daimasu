'use client';

import {useTranslations} from 'next-intl';
import Image from 'next/image';

export default function Place() {
  const t = useTranslations(); // Assumes i18n setup is already in place

  return (
    <section className="relative w-full h-auto py-14 sm:py-20 overflow-hidden bg-transparent -mt-[200px]">
      {/* Background image */}
    </section>
  );
}
